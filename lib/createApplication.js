import createApplication from './internal/createApplication';

// IdentService.create_application(api_token, {
//     network_id: network_id,
//     name: app_name,
//     config: {
//       network_id: network_id,
//     },
//   })

export default createApplication(token, params);
